import { Container, Section } from "../GlobalStyle";

const QuizCatalog = () => {
  return (
    <Section>
      <Container>
        <div>QuizCatalog</div>
      </Container>
    </Section>
  );
};

export default QuizCatalog;
